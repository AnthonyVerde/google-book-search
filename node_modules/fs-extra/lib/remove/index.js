'use strict'

const u = require('fs-extra/lib/remove/node_modules/universalify').fromCallback
const rimraf = require('fs-extra/lib/remove/rimraf')

module.exports = {
  remove: u(rimraf),
  removeSync: rimraf.sync
}
